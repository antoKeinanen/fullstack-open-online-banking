{
  "name": "@repo/protobufs",
  "scripts": {
    "generate-protobuf": "mkdir -p ./generated/go && protoc --go_out=generated/go --go_opt=paths=source_relative --go-grpc_out=generated/go --go-grpc_opt=paths=source_relative tigerbeetle-service.proto",
    "install": "go mod tidy"
  }
}