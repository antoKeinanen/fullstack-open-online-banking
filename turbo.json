{
  "$schema": "https://turborepo.com/schema.json",
  "ui": "tui",
  "tasks": {
    "generate-protobuf": {
      "outputs": [
        "*.go"
      ],
      "inputs": [
        "**/*.proto"
      ]
    },
    "dev": {
      "dependsOn": [
        "^generate-protobuf"
      ]
    }
  }
}